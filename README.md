Deployed using Railway

Don't feel like giving away money for hosting and api usage, so you can host it yourself & use your own OpenAI API key (set `OPENAI_API_KEY`).

ENV VARIABLES

MENTRAOS_API_KEY=

OPENAI_API_KEY=

OPENAI_MODEL= (defaults to GPT 5.2 Instant)

PACKAGE_NAME
