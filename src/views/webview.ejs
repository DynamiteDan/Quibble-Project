<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quibble Companion</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Quibble Companion</h1>
        </header>
        <div class="instructions">
            <% if (userId) { %>
                <p>Logged in as: <span class="user-id"><%= userId %></span></p>
            <% } else { %>
                <p>Not logged in</p>
            <% } %>
        </div>
    </div>
</body>
</html>